# A股短线做T量化 Web 应用 - 整体计划

## 1. 项目目标

构建一个可开箱即用的量化做T系统，覆盖 A 股全部股票与 ETF，基于实时行情 + 量化指标 + 大模型分析，给出日内正T/反T建议（买卖点、仓位、止损、风险评级），并通过交易制度识别、多周期共振、量价结构、板块联动、事件风控、AI 复盘与回测能力提升策略胜率与稳定性。

## 2. 范围与交付

### 2.1 必须交付

1. 前端 Web（React + ECharts）：
- 自选股实时监控面板
- 个股量化 + AI 分析页面
- 系统配置页面（LLM / DB / 数据源）

2. 后端 API（FastAPI）：
- 行情拉取（默认免费数据源）
- 全市场标的管理（股票 + ETF）
- 做T量化策略引擎（正T/反T）
- 大模型分析整合（OpenAI 兼容）
- 胜率增强模块（多周期/板块/事件/仓位/成本/纪律）
- 配置持久化（SQLite 默认）

3. 数据层：
- 默认 SQLite
- 支持通过配置切换外部数据库 URL

4. 文档：
- 启动说明
- 接口说明
- 配置说明
- 胜率增强功能说明
- 风险提示与合规边界

### 2.2 本期增强功能（全部纳入）

- 可交易性过滤器（成交额、点差、振幅、量比、流动性）
- 交易制度识别器（股票 T+1、ETF T+0/T+1 差异化处理）
- 多周期共振分析（1m / 5m / 15m / 日线）
- VWAP + 量价结构引擎
- 板块联动评分与指数同步性分析
- 分时场景策略库（开盘、午前、午后、尾盘）
- 信号质量评分与准入阈值
- 动态仓位引擎
- 交易成本与滑点模拟
- 事件风险过滤（公告、停复牌、异动、财报窗口）
- 盘口微结构插件接口（逐笔/委买委卖/大单）
- AI 复盘系统
- 回测与 Walk-forward 验证
- 情绪与纪律风控（日内止损、连亏熔断、降频）

### 2.3 非本期范围（可后续扩展）

- 实盘交易下单
- 用户权限系统（多账户）
- 多策略自动轮询调度

## 3. 技术方案

### 3.1 技术栈

- 前端：React + TypeScript + Vite + ECharts
- 后端：Python FastAPI + SQLAlchemy
- 数据库：SQLite（默认）
- 数据源：默认免费 A 股接口（可切换）

### 3.2 系统架构（逻辑分层）

1. `DataSource Layer`
- 统一行情接口（实时、K线、标的列表）
- 默认实现：免费接口适配器
- 增强实现：板块、公告、ETF 规则、可选盘口接口
- 预留：可切换自定义接口

2. `Market Rules Layer`
- 识别股票、ETF、跨境 ETF、债券 ETF 等交易制度
- 输出是否允许当日回转、是否适配正T/反T逻辑

3. `Feature Engine Layer`
- 指标计算：RSI、MACD、均线、量比、VWAP 偏离、波动率等
- 衍生特征：多周期趋势、分时场景、板块联动、成本/滑点评估、事件风险

4. `Quant Engine Layer`
- 先做可交易性过滤
- 再做信号评分与策略判断
- 输出正T、反T、观望以及仓位/止损建议

5. `AI Layer`
- 输入：行情快照 + 指标 + 策略初判
- 输出：可解释建议（理由、风险补充、执行注意事项、复盘总结）
- 兼容任意 OpenAI 风格 API

6. `Research Layer`
- 记录信号、执行、复盘结果
- 提供样本外回测与胜率分析

7. `API Layer`
- 面向前端的统一业务接口

8. `Web UI Layer`
- 监控面板 / 分析页 / 配置页 / 复盘与研究页

## 4. 模块拆解与里程碑

## 4.1 M0：交易策略先行（新增）

- 新增 `trading-quant-lead` 角色，负责在产品定义前给出策略与风控基线
- 先确定信号准入、仓位规则、止损纪律、回测验收阈值
- 产出“策略约束清单”，作为产品与工程的先决输入

验收标准：
- 明确正T/反T触发条件和禁止交易条件
- 明确回测最低门槛（胜率/盈亏比/最大回撤）

## 4.2 M1：后端基础设施（Day 1）

- FastAPI 工程搭建
- SQLite 初始化与表结构
- 配置管理模块
- 健康检查 API

验收标准：
- 服务可启动
- DB 自动建表
- 配置可读写

## 4.3 M2：行情与标的能力（Day 1-2）

- 标的池拉取（A股 + ETF）
- 实时行情接口
- 分时/K线接口
- 板块与指数映射
- ETF 规则与交易制度识别
- 自选股增删改查

验收标准：
- 可查询任意代码行情
- 自选列表可实时刷新

## 4.4 M3：量化做T策略引擎（Day 2）

- 指标计算模块
- 可交易性过滤
- 多周期共振
- VWAP + 量价结构
- 分时场景策略
- 做T打分与策略选择
- 输出：正T/反T、点位、仓位、止损、风险等级、信号质量分

验收标准：
- 对单只股票可稳定输出结构化建议

## 4.5 M4：风控与胜率增强（Day 2-3）

- 板块联动评分
- 成本与滑点模拟
- 事件风险过滤
- 动态仓位引擎
- 情绪与纪律风控
- 盘口微结构插件接口

验收标准：
- 分析结果能给出“适合/不适合做T”的明确判断
- 高风险场景会自动降仓或禁做

## 4.6 M5：AI 分析整合（Day 3）

- LLM 配置（Key/URL/Model）
- 提示词模板与 JSON 结构化输出
- 量化建议与 AI 结论融合
- AI 复盘总结

验收标准：
- 未配置 API 时可回退量化建议
- 配置 API 后可输出 AI 解释增强结果

## 4.7 M6：研究与验证（Day 3-4）

- 信号日志与结果归档
- 策略回测
- Walk-forward 样本外验证
- 胜率、盈亏比、最大回撤统计

验收标准：
- 策略能查看历史信号表现
- 可对参数进行样本外验证

## 4.8 M7：前端页面（Day 4）

- 监控面板
- 分析页（图表 + 指标 + 建议）
- 配置页（全开放配置）
- 研究/复盘页面

验收标准：
- 页面完整可用，移动端/桌面端可访问

## 4.9 M8：联调与文档（Day 4）

- 前后端联调
- 错误处理与空状态
- README 与部署说明

验收标准：
- 一键按文档启动
- 关键流程可演示

## 5. 接口规划（概要）

1. 市场/标的
- `GET /api/instruments`
- `POST /api/instruments/sync`
- `GET /api/quote/{symbol}`
- `GET /api/kline/{symbol}`
- `GET /api/instruments/{symbol}/rules`
- `GET /api/sectors/{symbol}`

2. 自选股
- `GET /api/watchlist`
- `POST /api/watchlist`
- `DELETE /api/watchlist/{symbol}`
- `GET /api/watchlist/quotes`

3. 分析
- `POST /api/analyze`
- `POST /api/analyze/batch`

4. 风控与研究
- `GET /api/replay/signals`
- `POST /api/backtest/run`
- `GET /api/backtest/{run_id}`
- `GET /api/market-events`

5. 配置
- `GET /api/settings`
- `PUT /api/settings`

## 6. 数据模型（概要）

- `instruments`：标的主表（symbol、name、market、type）
- `instrument_rules`：交易制度、做T限制、可回转规则
- `watchlist`：自选池（symbol）
- `system_settings`：系统配置（key-value）
- `analysis_logs`：分析日志留存
- `signal_replays`：信号结果回放
- `backtest_runs`：回测结果
- `market_events`：事件风险缓存

## 7. 风险与边界

- 本系统为研究/辅助决策工具，不构成投资建议。
- A 股存在 T+1、涨跌停、流动性与滑点等交易约束，股票与 ETF 的回转规则并不完全一致。
- 免费数据源可能存在延迟、限频、字段变化，需做兜底。
- 盘口微结构、逐笔和高级订单流数据在免费源下可能不完整，系统应支持自动降级。
- AI 输出可能存在偏差，必须结合风控阈值执行。

## 8. 成功标准（Definition of Done）

1. 项目可本地一键启动（前后端）。
2. 支持股票/ETF 的搜索与行情展示。
3. 自选股可实时监控并展示做T信号。
4. 分析页可输出正T/反T建议、信号质量分及风控参数。
5. 系统可识别交易制度并在不适合做T时明确拦截。
6. 系统配置页可保存并生效（或提示重启生效）。
7. 复盘/回测页可查看胜率、盈亏比和回撤。
8. 文档完整，首次拉起不需要额外口头说明。
