# 项目执行计划

## 已完成主线

1. 统一登录和用户隔离
   - Web/App 共用后端认证。
   - 自选、模拟盘和 App 接口按用户隔离。
   - 模拟盘白名单通过 `can_paper_trade` 控制。

2. 低吸策略重组
   - 下架泛低吸旧入口。
   - 保留首板回调、量能低吸和 3 个主线/中军/收盘承接策略。
   - 旧策略保留研究兼容和回测对照，不进入生产强买口径。

3. 模拟盘
   - 模拟委托、成交、持仓、账户、绩效看板、自动交易。
   - 自动交易开启时禁用手工委托。
   - 风控熔断支持连续亏损暂停账户。

4. Agent 能力层
   - Agent Safe API、Tool Registry、Provider 抽象、Hermes 接入。
   - 只读工具默认可用，写/通知工具默认关闭。

5. 运维与安全
   - 生产部署脚本、备份 cron、证书自动化、清理脚本。
   - 核心路由认证、速率限制、请求体限制、SSE 短令牌。

## 当前执行原则

- 交易策略逻辑不能因为性能优化而放宽。
- API 读物化快照，后台任务负责重计算。
- 前端优先展示可执行信息，复杂解释放入详情或弹窗。
- 新策略必须声明样本池、所需特征、回测口径和失效条件。

## 后续固定迭代

### 每日

- 收盘后归档模拟盘绩效。
- 生成策略和市场状态绩效。
- 刷新低吸策略物化快照。

### 每月

- 对生产策略执行样本外验证。
- 输出 `keep_production / observe / downgrade` 建议。
- 复核策略是否仍满足生产层门槛。

### 每次上线前

- `python3 -m compileall backend/app`
- `PYTHONPATH=backend backend/.venv/bin/python -m unittest discover backend/tests`
- `cd frontend && npm run build`
- 云端 `/readyz` 和核心 API 冒烟。

## 未开放能力

- 真实券商下单。
- 自动修改生产策略阈值。
- Agent 自动写策略参数。
- Agent 直接访问数据库或服务器 shell。

这些能力必须单独设计审批和风控后才能开放。
