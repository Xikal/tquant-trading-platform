# A 股短线策略体系重组方案

## 目标

将当前 12 策略体系重组为 **4 核心 + 6 辅助 + 2 因子 + 3 新策略** 的清晰分层架构。每项改动精确到文件名和代码行。

---

## 第一部分：策略分层重定义

### 1.1 新的四层架构

```
Layer 0 (核心生产) → 优先榜主力   first_board, classic_retrace, volume_shrink, divergence_consensus
Layer 1 (辅助生产) → 优先榜补充   ma_support, breakout_support, late_session_strong_support
Layer 2 (研究验证) → 研究面板     limit_up_breakout_retrace, core_midcap_vwap_ma5_retrace, sector_mainline_first_divergence
Layer F (因子层)   → 不对用户展示 deep_pullback_factor, trend_rebound_factor
Layer N (新策略)   → 实验面板     板块轮动低吸, 尾盘抢筹确认, 冰点修复低吸
```

### 1.2 每层策略的处理方式

| 层级 | 策略 | 出现在优先榜？ | 独立信号？ | scoring 参与方式 |
|---|---|---|---|---|
| 核心 | first_board | 是 | 是 | 全权重独立排名 |
| 核心 | classic_retrace | 是 | 是 | 全权重独立排名 |
| 核心 | volume_shrink | 是 | 是 | 全权重独立排名 |
| 核心 | divergence_consensus | 是 | 是 | 全权重独立排名 |
| 辅助 | ma_support | 是 | 是 | 0.65x 权重参与排名 |
| 辅助 | breakout_support | 是 | 是 | 0.65x 权重参与排名 |
| 辅助 | late_session_strong_support | 是 | 否(仅near_entry) | 0.5x 权重，不给 buy_now |
| 研究 | limit_up_breakout_retrace | 否 | 是 | 仅研究面板展示 |
| 研究 | core_midcap_vwap_ma5_retrace | 否 | 是 | 仅研究面板展示 |
| 研究 | sector_mainline_first_divergence | 否 | 是 | 仅研究面板展示 |
| 因子 | deep_pullback | 否 | 否 | 作为核心/辅助策略的 risk_aware_bonus |
| 因子 | trend_rebound | 否 | 否 | 作为核心/辅助策略的 momentum_bonus |

---

## 第二部分：具体代码改动

### 改动 1：`strategy_policy.py` — 重分层

**文件**：`backend/app/services/low_buy/strategy_policy.py`

**现状**：
```python
PRODUCTION_PRIORITY_STRATEGIES = frozenset({"first_board", "volume_shrink", "late_session_strong_support",
    "core_midcap_vwap_ma5_retrace", "sector_mainline_first_divergence_low_buy"})
OBSERVATION_STRATEGIES = frozenset({"classic_retrace", "ma_support", "breakout_support", "trend_rebound"})
RESEARCH_STRATEGIES = frozenset({"limit_up_breakout_retrace", "divergence_consensus", "deep_pullback"})
```

**改为**：
```python
# Layer 0: 核心生产策略 — 优先榜主力，全权重独立排名
CORE_PRODUCTION_STRATEGIES = frozenset({
    "first_board",
    "classic_retrace",
    "volume_shrink",
    "divergence_consensus",
})

# Layer 1: 辅助生产策略 — 优先榜补充，降权参与排名
AUXILIARY_PRODUCTION_STRATEGIES = frozenset({
    "ma_support",
    "breakout_support",
    "late_session_strong_support",
})

# Layer 2: 研究验证策略 — 仅研究面板展示，不参与优先榜
RESEARCH_STRATEGIES = frozenset({
    "limit_up_breakout_retrace",
    "core_midcap_vwap_ma5_retrace",
    "sector_mainline_first_divergence_low_buy",
})

# Layer F: 因子策略 — 不对用户展示，只作为核心/辅助策略的加分因子
FACTOR_STRATEGIES = frozenset({
    "deep_pullback_factor",
    "trend_rebound_factor",
})

# 全部策略（不含 factor）
ALL_ACTIVE_STRATEGIES = (
    CORE_PRODUCTION_STRATEGIES
    | AUXILIARY_PRODUCTION_STRATEGIES
    | RESEARCH_STRATEGIES
)

# 参与优先榜的策略（核心 + 辅助）
PRIORITY_BOARD_STRATEGIES = CORE_PRODUCTION_STRATEGIES | AUXILIARY_PRODUCTION_STRATEGIES

# buy_now/soft_buy_now 暂停的策略（辅助层 + 研究层）
STRONG_BUY_PAUSED_STRATEGIES = frozenset({
    "ma_support",
    "breakout_support",
    "late_session_strong_support",
    "limit_up_breakout_retrace",
    "core_midcap_vwap_ma5_retrace",
    "sector_mainline_first_divergence_low_buy",
})

# 兼容旧 key
STRATEGY_ALIASES.update({
    "deep_pullback": "deep_pullback_factor",
    "trend_rebound": "trend_rebound_factor",
})


def participates_in_priority_board(strategy_key: str) -> bool:
    return strategy_key in PRIORITY_BOARD_STRATEGIES


def is_core_production(strategy_key: str) -> bool:
    return strategy_key in CORE_PRODUCTION_STRATEGIES


def is_factor_strategy(strategy_key: str) -> bool:
    return strategy_key in FACTOR_STRATEGIES


def priority_layer_weight(strategy_key: str) -> float:
    """策略在优先榜中的权重系数"""
    if strategy_key in CORE_PRODUCTION_STRATEGIES:
        return 1.0
    if strategy_key in AUXILIARY_PRODUCTION_STRATEGIES:
        return 0.65
    return 0.0
```

---

### 改动 2：`strategy_pool_config.py` — 移除降级策略的池配置

**文件**：`backend/app/services/low_buy/strategy_pool_config.py`

**操作**：
1. 删除 `_STRATEGY_POOL_PROFILES` 中 `deep_pullback` 和 `trend_rebound` 条目（第 59-100 行对应部分）
2. 将 `classic_retrace` 的 `pool_key` 从 `"legacy_research_pool"` 改为 `"limit_up_pool"`，移除研究池标记
3. 将 `divergence_consensus` 的 `pool_key` 从 `"event_research_pool"` 改为 `"limit_up_pool"`
4. 将 `core_midcap_vwap_ma5_retrace` 和 `sector_mainline_first_divergence_low_buy` 保持 `uses_daily_scan=True` 但标记为研究层

```python
_STRATEGY_POOL_PROFILES: dict[str, StrategyPoolProfile] = {
    # === Layer 0: 核心生产 ===
    "first_board": StrategyPoolProfile(
        strategy_key="first_board", pool_key="limit_up_event_pool",
        title="首板启动池", source="limit_up_pool",
    ),
    "classic_retrace": StrategyPoolProfile(
        strategy_key="classic_retrace", pool_key="classic_retrace_pool",
        title="经典低吸池", source="limit_up_pool",
    ),
    "volume_shrink": StrategyPoolProfile(
        strategy_key="volume_shrink", pool_key="volume_contract_pool",
        title="放量启动后缩量回踩池", source="limit_up_pool",
    ),
    "divergence_consensus": StrategyPoolProfile(
        strategy_key="divergence_consensus", pool_key="divergence_consensus_pool",
        title="分歧转一致池", source="limit_up_pool",
    ),
    # === Layer 1: 辅助生产 ===
    "ma_support": StrategyPoolProfile(
        strategy_key="ma_support", pool_key="ma_support_pool",
        title="均线支撑池", source="limit_up_pool",
    ),
    "breakout_support": StrategyPoolProfile(
        strategy_key="breakout_support", pool_key="breakout_support_pool",
        title="位置支撑池", source="limit_up_pool",
    ),
    "late_session_strong_support": StrategyPoolProfile(
        strategy_key="late_session_strong_support", pool_key="mainline_daily_pool",
        title="收盘强势承接池", source="daily_history",
        max_size=360, uses_daily_scan=True,
    ),
    # === Layer 2: 研究验证 ===
    "limit_up_breakout_retrace": StrategyPoolProfile(
        strategy_key="limit_up_breakout_retrace", pool_key="event_research_pool",
        title="涨停突破回踩研究池", source="limit_up_pool",
    ),
    "core_midcap_vwap_ma5_retrace": StrategyPoolProfile(
        strategy_key="core_midcap_vwap_ma5_retrace", pool_key="mainline_daily_pool",
        title="中军VWAP回踩研究池", source="daily_history",
        max_size=360, uses_daily_scan=True,
    ),
    "sector_mainline_first_divergence_low_buy": StrategyPoolProfile(
        strategy_key="sector_mainline_first_divergence_low_buy", pool_key="mainline_daily_pool",
        title="主线首分歧研究池", source="daily_history",
        max_size=360, uses_daily_scan=True,
    ),
    # === Layer N: 新策略（后续添加）===
    # "sector_rotation_low_buy": StrategyPoolProfile(...)
    # "late_session_accumulation": StrategyPoolProfile(...)
    # "ice_point_recovery": StrategyPoolProfile(...)
}
```

---

### 改动 3：`market_state_rules.py` — 新增 directional_bias + 清理因子策略 override

**文件**：`backend/app/services/low_buy/market_state_rules.py`

**操作 A**：从 `_STRATEGY_OVERRIDES` 中删除 `deep_pullback`（第 217-224 行）和 `trend_rebound`（第 225-232 行）

**操作 B**：在 `_STRATEGY_OVERRIDES` 中为 `classic_retrace` 增加 `broad_rally`、`low_volume_wait`、`fast_rotation`、`risk_release` 四个状态的 override：当前 classic_retrace 只定义了 repair/weight_support/weight_support_active/high_flyer_retreat 四个 override，需补全。类似地为 `divergence_consensus` 补充 repair 状态的向上修正。

**操作 C**：在文件末尾新增 `directional_bias` 函数：

```python
@dataclass(frozen=True)
class DirectionalBias:
    """今日做 T 倾向：正 T / 反 T / 观望"""
    bias: str  # "positive_t", "negative_t", "neutral"
    label: str
    strength: float  # 0.0-1.0
    reason: str
    preferred_strategies: list[str]
    avoided_strategies: list[str]


def compute_directional_bias(
    market_state: str,
    market_state_strength: float,
    emotion: MarketEmotionSnapshot | None = None,
    hot_industries_count: int = 0,
    limit_up_count: int = 0,
    limit_down_count: int = 0,
) -> DirectionalBias:
    """根据市场状态、情绪和广度计算今日做 T 倾向。
    
    核心逻辑：
    - broad_rally + 涨停>50 + 热点≥3 → positive_t（优先正 T 和低吸）
    - high_flyer_retreat + 跌停>30 → negative_t（优先反 T）
    - risk_release / 涨停<20 → neutral（观望为主）
    - 其余情况综合判断
    """
    # 情绪极端检测
    is_ice_point = (limit_up_count < 20 and limit_down_count > 30 and 
                    emotion is not None and emotion.emotion_ready)
    
    # 方向判断
    if market_state == "risk_release":
        return DirectionalBias(
            bias="neutral", label="风险释放，以观望为主",
            strength=0.95,
            reason="市场处于风险释放期，低吸和做 T 风险均偏高。",
            preferred_strategies=[],
            avoided_strategies=list(ALL_ACTIVE_STRATEGIES),
        )
    
    if is_ice_point:
        return DirectionalBias(
            bias="positive_t", label="冰点修复，关注抗跌股",
            strength=0.72,
            reason="涨停骤减+跌停激增，市场进入情绪冰点，次日修复概率偏高。关注昨日抗跌股的低吸机会。",
            preferred_strategies=["classic_retrace", "volume_shrink", "ma_support"],
            avoided_strategies=["divergence_consensus", "limit_up_breakout_retrace", 
                               "late_session_strong_support"],
        )
    
    if market_state == "high_flyer_retreat":
        return DirectionalBias(
            bias="negative_t", label="高标退潮，优先防守",
            strength=0.85,
            reason="高标退潮期，打板策略风险极高，优先反 T 防守和主线中军回踩。",
            preferred_strategies=["core_midcap_vwap_ma5_retrace", "ma_support"],
            avoided_strategies=["first_board", "limit_up_breakout_retrace", 
                               "divergence_consensus", "late_session_strong_support"],
        )
    
    if market_state == "broad_rally" and limit_up_count >= 50 and hot_industries_count >= 3:
        return DirectionalBias(
            bias="positive_t", label="市场扩散，优先低吸",
            strength=0.80,
            reason="市场宽度好、涨停家数多、主线明确，适合积极做正 T 和低吸。",
            preferred_strategies=["first_board", "classic_retrace", "volume_shrink", 
                                 "divergence_consensus"],
            avoided_strategies=[],
        )
    
    if market_state == "fast_rotation":
        return DirectionalBias(
            bias="neutral", label="轮动过快，只做最强",
            strength=0.65,
            reason="热点轮动过快，追高容易吃套。只保留最强结构和位置的机会。",
            preferred_strategies=["volume_shrink", "breakout_support"],
            avoided_strategies=["first_board", "late_session_strong_support", 
                               "sector_mainline_first_divergence_low_buy"],
        )
    
    # 默认：中性
    return DirectionalBias(
        bias="neutral", label="中性环境，按结构选股",
        strength=0.40,
        reason="市场状态中性，优先看结构而非方向。",
        preferred_strategies=[],
        avoided_strategies=[],
    )
```

---

### 改动 4：`dynamic_adjustments.py` — 引入因子加成

**文件**：`backend/app/services/low_buy/dynamic_adjustments.py`

**在 `low_buy_dynamic_adjustment()` 函数中新增因子加成逻辑**：

```python
def low_buy_dynamic_adjustment(
    *,
    market_regime: MarketRegimeSnapshot | None,
    risk_tier: str,
    leader_rank: str,
    performance: LowBuyStrategyPerformanceOut | None = None,
    factor_bonuses: dict[str, float] | None = None,  # 新增参数
) -> LowBuyDynamicAdjustment:
    # ... 现有逻辑保持不变 ...
    
    # 新增：因子加成
    if factor_bonuses:
        deep_pullback_bonus = factor_bonuses.get("deep_pullback_factor", 0.0)
        trend_rebound_bonus = factor_bonuses.get("trend_rebound_factor", 0.0)
        
        # 深度回踩因子：risk_tier 为 note 且回撤深度达标时，给予风险补偿加成
        if deep_pullback_bonus > 0 and risk_tier == "note":
            score_floor_shift -= min(deep_pullback_bonus * 0.6, 1.8)
            position_multiplier *= 0.88  # 深度回踩仓位保守
            reason_parts.append(f"深度回踩补偿 +{deep_pullback_bonus:.1f}(仓位保守)")
        
        # 趋势龙回头因子：在前主升浪确认时给予动量加成
        if trend_rebound_bonus > 0:
            score_floor_shift -= min(trend_rebound_bonus * 0.8, 2.0)
            position_multiplier *= 1.06
            reason_parts.append(f"龙回头动量 +{trend_rebound_bonus:.1f}")
    
    # ... 后续逻辑保持不变 ...
```

---

### 改动 5：`signal_family.py` — 新增因子评分函数

**文件**：`backend/app/services/low_buy/signal_family.py`

**在文件末尾新增两个因子评分函数**：

```python
def evaluate_deep_pullback_factor(
    metrics: CandidateMetrics,
    candidate_score: float,
) -> float:
    """深度回踩因子：评估候选股是否具备'深度回踩+错杀'特征。
    
    返回 0.0-5.0 的加分值。仅在以下条件同时满足时有效：
    - 回撤超过 -5%（真正的深度回踩）
    - 缩量明显（非放量杀跌）
    - 仍在 20 日线上方（趋势未坏）
    - 非 ST/问题股
    """
    if metrics.drawdown_from_board_pct > -5.0:
        return 0.0  # 回撤不够深，不属于深度回踩
    if not metrics.shrink_ok:
        return 0.0  # 放量杀跌，不是错杀
    if not metrics.trend_ok:
        return 0.0  # 趋势已坏
    
    # 加分 = 回撤深度分 + 缩量质量分 + 趋势强度分
    drawdown_score = _normalize(abs(metrics.drawdown_from_board_pct), 5.0, 12.0) * 2.0
    shrink_score = (1.0 if metrics.shrink_ok else 0.0) * 1.5
    trend_score = (1.0 if metrics.strong_trend else 0.5) * 1.5
    
    return round(_clamp(drawdown_score + shrink_score + trend_score, 0.0, 5.0), 2)


def evaluate_trend_rebound_factor(
    metrics: CandidateMetrics,
    item: BoardCandidate,
) -> float:
    """趋势龙回头因子：评估候选股是否具备'前主升浪+N字回拉'特征。
    
    返回 0.0-5.0 的加分值。仅在以下条件同时满足时有效：
    - board_count 1-3（有明确的启动板）
    - 回调不破 MA20（趋势未坏）
    - 近 3 日有止跌迹象
    - 量能没有失控放大
    """
    if item.board_count < 1 or item.board_count > 3:
        return 0.0
    if not metrics.trend_ok:
        return 0.0
    if metrics.latest_change_pct < -3.0:
        return 0.0  # 仍在快速下跌，未见止跌
    if not metrics.shrink_basic_ok:
        return 0.0
    
    # 加分 = 板数质量分 + 回调健康分 + 止跌确认分
    board_score = {1: 2.0, 2: 1.5, 3: 0.8}.get(item.board_count, 0.0)
    pullback_score = (1.0 if metrics.shrink_ok else 0.5) * 1.5
    stabilization_score = (1.0 if metrics.latest_change_pct >= -1.2 else 0.5) * 1.5
    
    return round(_clamp(board_score + pullback_score + stabilization_score, 0.0, 5.0), 2)
```

---

### 改动 6：`candidate.py` — 在候选评估中接入因子加成

**文件**：`backend/app/services/low_buy/candidate.py`

**在 `_evaluate_candidate()` 方法中**（约第 80-120 行），在 score 计算后、dynamic_adjustment 计算前，插入因子评估：

```python
# 在 score 计算完成后，dynamic_adjustment 计算之前插入：

# 因子加成评估（不对用户展示独立策略，只参与 scoring）
factor_bonuses: dict[str, float] = {}
deep_pullback_score = evaluate_deep_pullback_factor(metrics, candidate.score)
if deep_pullback_score > 0:
    factor_bonuses["deep_pullback_factor"] = deep_pullback_score
    candidate.score += deep_pullback_score * 0.30  # 因子仅贡献 30% 权重到主分

trend_rebound_score = evaluate_trend_rebound_factor(metrics, item)
if trend_rebound_score > 0:
    factor_bonuses["trend_rebound_factor"] = trend_rebound_score
    candidate.score += trend_rebound_score * 0.30

# 将 factor_bonuses 传入 dynamic_adjustment
adjustment = low_buy_dynamic_adjustment(
    market_regime=...,
    risk_tier=...,
    leader_rank=...,
    performance=...,
    factor_bonuses=factor_bonuses,  # 新增
)
```

---

### 改动 7：`holding_policy.py` — 更新持有策略

**文件**：`backend/app/services/low_buy/holding_policy.py`

**操作**：
1. 修改 `classic_retrace` 的 policy（从"观察策略"升级为"核心策略"）：

```python
"classic_retrace": StrategyHoldingPolicy(
    max_holding_days=3,
    brief="1-3日验证",
    time_stop_text="T+2 不能脱离买点区或转强，先降级；强势票可验证到第 3 天。",
    card_hint="持有建议：1-3日验证，冲高先降风险，跌破止损退出。",
),
```

2. 修改 `divergence_consensus` 的 policy（从"研究层"升级为"核心策略"）：

```python
"divergence_consensus": StrategyHoldingPolicy(
    max_holding_days=2,
    brief="T+1弱转强验证",
    time_stop_text="T+1 必须弱转强确认，放量站上分歧高点持有，T+2 不强于板块退出。",
    card_hint="持有建议：T+1弱转强确认，放量站上分歧高点才能加仓。",
),
```

3. 删除 `deep_pullback` 和 `trend_rebound` 的独立 policy（它们现在是因子）

---

### 改动 8：`shared.py` PLAYBOOKS — 更新策略文案

**文件**：`backend/app/services/low_buy/shared.py`

**操作**：
1. 将 `classic_retrace` 和 `divergence_consensus` 的 `notes` 从"观察/研究"语气改为"生产"语气
2. 从 PLAYBOOKS 中移除 `deep_pullback` 和 `trend_rebound`（它们不再独立展示）
3. 将 `late_session_strong_support`、`core_midcap_vwap_ma5_retrace`、`sector_mainline_first_divergence_low_buy` 的文案标记为研究层

---

### 改动 9：前端 `workspaceConstants.ts` — 更新 Tab 展示

**文件**：`frontend/src/features/trading-workspace/workspaceConstants.ts`

```typescript
// 核心策略 Tab（优先榜展示）
export const CORE_PLAYBOOK_TABS = [
  { key: "first_board", label: "首板回调" },
  { key: "classic_retrace", label: "经典低吸" },
  { key: "volume_shrink", label: "量能低吸" },
  { key: "divergence_consensus", label: "分歧转一致" },
] as const;

// 辅助策略 Tab
export const AUXILIARY_PLAYBOOK_TABS = [
  { key: "ma_support", label: "均线支撑" },
  { key: "breakout_support", label: "位置支撑" },
  { key: "late_session_strong_support", label: "收盘强势承接" },
] as const;

// 研究策略 Tab（折叠在"研究"下拉中）
export const RESEARCH_PLAYBOOK_TABS = [
  { key: "limit_up_breakout_retrace", label: "涨停突破回踩" },
  { key: "core_midcap_vwap_ma5_retrace", label: "中军VWAP回踩" },
  { key: "sector_mainline_first_divergence_low_buy", label: "主线首分歧" },
] as const;

export const ALL_PLAYBOOK_TABS = [
  ...CORE_PLAYBOOK_TABS,
  ...AUXILIARY_PLAYBOOK_TABS,
  ...RESEARCH_PLAYBOOK_TABS,
] as const;
```

---

### 改动 10：`priority_scoring.py` — 接入 directional_bias 和因子加成

**文件**：`backend/app/services/low_buy/priority_scoring.py`

**在 `_environment_rank_bonus()` 方法中增加 directional_bias 的判断**：

```python
def _environment_rank_bonus(
    self,
    candidate: LowBuyCandidateOut,
    market_context: PriorityMarketContext,
) -> float:
    market_bonus = self._market_state_bonus(candidate, market_context)
    strategy_bonus = self._strategy_rank_tilt(candidate, market_context)
    sector_bonus = self._sector_rotation_bonus(candidate, market_context)
    
    # 新增：directional_bias 约束
    directional_penalty = self._directional_bias_penalty(candidate, market_context)
    
    return round(max(-4.5, min(4.5, market_bonus + strategy_bonus + sector_bonus * 0.85 + directional_penalty)), 2)

def _directional_bias_penalty(
    self,
    candidate: LowBuyCandidateOut,
    market_context: PriorityMarketContext,
) -> float:
    """根据市场方向倾向对策略做加权/降权"""
    if market_context.directional_bias == "neutral":
        return -1.5 if candidate.strategy_key in market_context.avoided_strategies else 0.0
    if market_context.directional_bias == "positive_t":
        if candidate.strategy_key in market_context.preferred_strategies:
            return 1.5
        if candidate.strategy_key in market_context.avoided_strategies:
            return -2.5
    if market_context.directional_bias == "negative_t":
        if candidate.strategy_key in market_context.preferred_strategies:
            return 0.8
        if candidate.strategy_key in market_context.avoided_strategies:
            return -3.0
    return 0.0
```

同时在 `PriorityMarketContext` dataclass（`priority_types.py`）中增加字段：

```python
@dataclass(frozen=True)
class PriorityMarketContext:
    # ... 现有字段 ...
    directional_bias: str = "neutral"        # "positive_t", "negative_t", "neutral"
    directional_bias_label: str = ""
    directional_bias_strength: float = 0.0
    preferred_strategies: tuple[str, ...] = ()
    avoided_strategies: tuple[str, ...] = ()
```

---

## 第三部分：三个新策略的设计

### 新策略 A：板块轮动低吸 `sector_rotation_low_buy`

**逻辑**：当某板块连续 2 日资金净流入且板块指数回踩 5 日线时，低吸板块内尚未大幅启动的跟风股。

**信号生成流程**：
1. 每日收盘后，从 `hot_industry_context.py` 获取板块热度排名
2. 筛选连续 2 日主力净流入 > 0 的板块
3. 对每个符合条件的板块，筛选板块内未涨停（非当天涨停）的个股
4. 候选条件：近 5 日涨幅 < 板块涨幅 + 5%（未启动），成交额 > 板块中位数
5. 买点 = 回踩 5 日线 1% 以内，止损 = 板块指数 10 日线下方 2%

**新增文件**：`backend/app/services/low_buy/sector_rotation.py`

**验证指标**：板块轮动胜率（跟风股在买入后 3 日内涨幅 > 板块涨幅的比例）、平均超额收益

---

### 新策略 B：尾盘抢筹确认 `late_session_accumulation`

**逻辑**：14:50-15:00 成交量放大至日均的 1.5 倍且价位在买点区内，资金在尾盘集中掃货。

**信号生成流程**：
1. 盘中实时监控已在低吸买点区内的候选股
2. 14:50-15:00 期间，量比 > 1.5 且价格站稳均价的候选股→触发尾盘抢筹信号
3. 信号格式：建议次日开盘关注，不追竞价
4. 次日开盘价 >= 昨日收盘价×0.998 → 可执行；否则观望

**新增文件**：`backend/app/services/low_buy/late_session_accumulation.py`

**修改文件**：`screening_quotes.py` 的盘中刷新增加尾盘抢筹检测

**验证指标**：尾盘信号次日高开概率、次日冲高 2% 以上概率

---

### 新策略 C：冰点修复低吸 `ice_point_recovery`

**逻辑**：市场情绪极值（涨停<20 且跌停>30）次日，低吸前日抗跌的优质股。

**信号生成流程**：
1. 每日收盘计算情绪冰点指标（已由 `emotion.py` 提供数据）
2. 冰点确认后，筛选前日收盘价 > 开盘价（收阳）且跌幅 < 3% 的个股（抗跌）
3. 进一步条件：非 ST、成交额 > 5000 万、在 20 日线上方
4. 买点 = 次日开盘后回踩昨日低点不破，止损 = 昨日最低价下方 2%
5. 信号有效期：仅冰点次日一天

**新增文件**：`backend/app/services/low_buy/ice_point_recovery.py`

**修改文件**：`emotion.py` 增加冰点状态导出接口

**验证指标**：冰点次日修复胜率、平均修复幅度、连续冰点后的累进修复概率

---

## 第四部分：验收标准与回归测试

### 4.1 策略验收矩阵

每个策略在上线前必须通过以下验收：

| 指标 | 核心策略阈值 | 辅助策略阈值 | 研究策略阈值 |
|---|---|---|---|
| 回测样本量（成交） | ≥ 80 | ≥ 40 | ≥ 20 |
| 净胜率 (net_win_rate) | ≥ 12% | ≥ 8% | ≥ 5% |
| 平均净收益 | ≥ 0.6% | ≥ 0.3% | ≥ 0.0% |
| 止损率 | ≤ 30% | ≤ 38% | ≤ 45% |
| 最大序列回撤 | ≤ 6% | ≤ 9% | ≤ 12% |
| 市场环境覆盖 | ≥ 5 种 | ≥ 3 种 | ≥ 2 种 |
| 月均信号数 | 8-25 | 5-30 | 不限 |

### 4.2 回归测试

每次策略调整后必须运行以下测试：

1. **全策略回测对比**：新旧版本在所有策略上运行相同的 60 天回测窗口，对比胜率和净收益变化
2. **市场环境覆盖测试**：验证每个策略在 8 种 market_state 下都有合理的表现
3. **边界条件测试**：极端行情（千股跌停、千股涨停、熔断级波动）下策略不崩溃
4. **因子贡献分解**：验证 deep_pullback_factor 和 trend_rebound_factor 对核心策略的增量贡献 ≥ 0.5% 胜率

### 4.3 回滚预案

- 新版策略配置通过 `LOW_BUY_RESULT_VERSION` 版本号区分（当前 v8 → 新版 v9）
- 旧版本物化结果保留 30 天不删除
- 前端通过 `strategy_layer` 字段决定展示位置，兼容旧版

---

## 第五部分：实施顺序

### 阶段 1（1 周）：分层定义
- [ ] 改动 1：`strategy_policy.py` 重分层
- [ ] 改动 2：`strategy_pool_config.py` 移除降级策略池
- [ ] 改动 7：`holding_policy.py` 更新策略
- [ ] 改动 9：前端 `workspaceConstants.ts` 更新 Tab

### 阶段 2（1 周）：因子化
- [ ] 改动 5：`signal_family.py` 新增因子评分函数
- [ ] 改动 6：`candidate.py` 接入因子加成
- [ ] 改动 4：`dynamic_adjustments.py` 接入因子加成

### 阶段 3（1 周）：directional_bias
- [ ] 改动 3：`market_state_rules.py` 新增 directional_bias
- [ ] 改动 10：`priority_scoring.py` 接入 directional_bias
- [ ] `priority_types.py` 新增字段

### 阶段 4（2-3 周）：新策略 MVP
- [ ] 新策略 A：板块轮动低吸
- [ ] 新策略 B：尾盘抢筹确认
- [ ] 新策略 C：冰点修复低吸

### 阶段 5（持续）：验收
- [ ] 全策略 60 天回测对比
- [ ] 因子贡献分解验证
- [ ] 边界条件测试
