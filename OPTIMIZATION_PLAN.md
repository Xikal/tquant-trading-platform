# A股短线做T量化平台优化方案（2026-04）

## 1. 目标定义

在保留“开箱即用”的前提下，将系统升级为可长期运行的生产形态：

- 默认可运行：免费行情 + SQLite
- 可平滑升级：外部 MySQL/PostgreSQL + 自定义大模型 + 自定义数据源
- 核心场景稳定：监控、分析、配置、回测四条主链路可持续使用

## 2. 问题复盘（上一版）

- 部署入口分散：`api/*` 存在大量重复包装函数，维护复杂、构建不稳定。
- 云端依赖敏感：无 `akshare` 场景下，同步全市场标的能力不足。
- 监控页脆弱：单标的数据异常可能影响整页刷新。
- 配置链路信息重复：分析结果里合规提示存在重复项。

## 3. 本轮优化顺序与验收

### 3.1 交易与策略先行（新增）

1. 新增 `trading-quant-lead` 角色，排在产品之前。
2. 先固化“可盈利前提”再进入产品设计：
- 可交易性过滤阈值
- 信号质量与准入阈值
- 仓位、止损止盈与日内纪律
- 回测与 Walk-forward 最低验收门槛

### 3.2 产品方案基线

1. 明确两种运行模式：
- 研究模式：默认 SQLite + 免费行情 + 可选 AI
- 生产模式：外部数据库 + 完整风控参数 + 部署脚本

2. 明确验收标准：
- 能拉起系统并完成一次完整做T分析
- 能在配置页完成数据库检测与迁移
- 能输出正T/反T/观望建议与风险等级

### 3.3 UI 与交互

1. 强化关键信息可见性：
- 信号、风险、仓位、点位优先展示
- 监控页行级错误提示

2. 强化容错体验：
- 明确加载态、失败态、空态
- 失败时降级为“观望”而不是整体报错

### 3.4 全栈能力

1. 数据源降级能力：
- `akshare` 不可用时，自动从内置种子库补齐股票/ETF 全市场标的

2. API 鲁棒性：
- 自选信号接口增加失败兜底
- 事件风控支持按请求维度开关
- 合规提示去重

3. 部署入口收敛：
- 统一 `api/index.py + api/[...path].py` 两个函数入口

### 3.5 QA 联调

1. 增加一键冒烟脚本：
- 构建前端
- 拉起后端
- 校验 `/healthz`、`/api/instruments`、`/api/analyze`、`/api/settings`、`/api/watchlist/signals`

2. 保留人工抽检：
- 监控页刷新
- 分析页图表与建议
- 配置页数据库检测
- 回测页输出

### 3.6 DevOps 收尾

1. 减少云端部署风险：
- 根依赖精简，避免无关重依赖进入函数
- API 路由统一到 catch-all

2. 生产化建议：
- 长期运行使用外部数据库
- 本地用 Navicat 管理 MySQL，仅作为可视化客户端

## 4. 风险边界

- 系统用于研究与辅助决策，不构成投资建议。
- 免费数据源可能存在延迟、限频与字段变化，策略需结合风控执行。
- AI 输出仅做解释增强，不覆盖交易制度和风险硬约束。
