# gupiao 系统级全方位提升方案

> 评审日期：2026-05-30　|　评审分支：`codex/phase4-phase5-architecture`
> 评审范围：只读全局检查（未修改任何业务代码、未重构、未删除文件）
> 证据基线：`understand-anything` 知识图谱（commit `29ae8a4`，分析 1712 文件）+ 当前分支源码逐项核验
> 规模基线：后端 Python ≈ 100,055 LOC；前端 TS/TSX ≈ 37,517 LOC；Go ≈ 4,002 LOC；Rust ≈ 464 LOC；后端测试 151 个、前端测试 24 个；API 路由文件 42 个；后台调度 loop 19 个；Alembic 迁移 34 个

---

## 1. 总体结论

**当前平台最大问题：不是“功能缺陷”，而是“系统表面积/复杂度”超过了平台真实使用规模（A 股短线辅助 + 模拟盘，不下真单）所需。**

平台已是一个高成熟度、且正在被认真治理的系统：
- 上一版自评报告（`全方位评估报告-2026-05-01.md`）列出的 4 个“立即修复”项 **已全部闭环**（见下方核验）。
- 回测口径在 2026-05-29 刚做过严肃收口（`near_entry` 不进生产收益排行、真实组合 max5/max10 口径、收益口径改名为“每日信号等权复利收益”）。
- 安全中间件、鉴权、审计、限流、加密、启动期安全校验齐全。

但与此同时：
- `low_buy` 单模块 ≈ 130 个文件，`services/` ≈ 100 个模块，19 个后台 loop，Python+Go+Rust 三栈，外加 ML/RL/因子挖掘/自进化/组合优化/套利研究/主力模型等大量“研究/模型治理”能力，且其中相当一部分在后台默认开启。
- 多份重叠的规划与评估文档（`IMPLEMENTATION_PLAN.md` 198KB、`OPTIMIZATION_PLAN.md`、`PROJECT_PLAN.md`、两份评估/重组方案等），`ARCHITECTURE.md`/`README.md` 已与代码漂移。

**核验：上一版 4 个 P0 已修复（不再作为本轮问题）**

| 旧编号 | 旧问题 | 当前状态 | 证据 |
|---|---|---|---|
| IM-1 | Agent 端点无鉴权 | 已修复 | [agent.py:80](backend/app/api/routes/agent.py:80) 路由级 `Depends(require_current_user_or_agent_token)` + 每端点 `require_agent_tool_permission(...)` |
| IM-2 | `net_win_rate==win_rate` | 已修复 | [performance.py:47](backend/app/services/paper/performance.py:47) `win_rate=wins/total`、`net_win_rate=(wins-losses)/total` 已区分 |
| IM-3 | Token 存 localStorage（XSS） | 已修复 | [auth.py:194](backend/app/api/routes/auth.py:194) `set_cookie(httponly=True, secure, samesite)`；前端 `credentials:"include"`，仅内存短期 access token（[base.ts:104](frontend/src/api/base.ts:104)），强 CSP 兜底（[main.py:43](backend/app/main.py:43)） |
| IM-4 | admin_auth 绕过 | 已修复 | [admin_auth.py:27](backend/app/core/admin_auth.py:27) 未配置 token 直接 503，`secrets.compare_digest` 比对 |

**结论性建议：**
- **是否继续扩功能：否（暂停扩张）。** 当前能力已远超使用规模，新增能力会进一步放大维护与运维风险。
- **是否先瘦身：是，优先瘦身 + 收口。** 但“瘦身”≠删代码，而是**默认关闭研究态、收敛文档、隔离后台、明确边界**。
- **当前适合的运行模式：研究 + 模拟盘双模式可上线；“干净生产模式”需先做第 1 阶段瘦身/隔离后再发布。** 平台本身不下真单，生产风险主要在“运维复杂度 + 研究功能误暴露 + 后台任务与 Web 争用”。
- **最优先方向：① 生产默认最小化（研究/模型治理 loop 默认关）；② Web 与后台/自动交易隔离；③ 文档与策略矩阵 SSOT 收敛；④ 限流多 worker 口径修复；⑤ 逐策略 24M OOS 样本充分性核验。**

---

## 2. 系统能力地图

> 依据 `domain-graph.json` 的 8 大域 + 源码核验。

### 2.1 核心生产能力（必须稳定、必须测试覆盖）
- **实时监控**：市场状态/候选/主线龙头/风险信号 + 前端监控台（`api/routes/monitor.py`、`services/monitor_*`、`features/monitor`）。
- **低吸选股（生产层）**：4 CORE + 4 AUXILIARY 策略、优先级榜、物化快照（`services/low_buy/*`、`strategy_policy.py`、`priority_board.py`）。
- **模拟盘 + 风控**：账户/委托/成交/自动交易/止盈止损/熔断/绩效归档（`api/routes/paper*.py`、`services/paper/*`，含 `risk_circuit.py`、`fees.py`、`scheduler.py`）。
- **行情数据管道**：多免费源路由 + 熔断 + 本地缓存（`services/market/providers/*`、`router.py`、`local_quote_cache`）。
- **24 个月回测 + 报告**：撮合/费用/滑点/涨跌停/T+1/前视守卫，JSON+Markdown（`services/backtest/*`、`scripts/strategy_24m_*`、`scripts/low_buy_market_backtest_*`）。
- **策略跟踪**：快照化、市场分段、shadow 观测（`services/strategy_tracking*`、`features/strategy-tracking`）。
- **设置/认证/部署**：双持久化、MFA/TOTP、加密、Docker/Compose（`core/config.py`、`services/auth_service.py`、`settings_*`）。

### 2.2 研究/实验能力（默认应不进普通用户入口）
- 因子挖掘（`services/factor_mining/*`、`api/routes/factor_mining.py`）。
- ML 止盈止损辅助模型（`services/ml_signal/*`，`paper_exit_model_*`）。
- 主力观测模型 / next-day event model / shadow（`low_buy/main_force_model_*`、`market_model_observation_service.py`）。
- 策略自进化 / 在线学习（`strategy_self_evolution.py`、`runtime/strategy_evolution_scheduler.py`、`evolution_*` 配置）。
- 深度 RL 仓位研究（`services/rl/*`、`enable_deep_rl*`，**默认关、依赖不入默认镜像**）。
- 组合优化与套利研究（`black_litterman_optimizer.py`、`markowitz_optimizer.py`、`portfolio_heuristic_optimizer.py`、`arbitrage_research.py`、`paired_hedge_research.py`、`position_policy_research.py`）。
- 另类数据（`alternative_data.py`）。

### 2.3 可选集成能力（应插件化/URL 开关，默认关）
- 飞书通知（`services/feishu/*`、`api/routes/feishu.py`、`notification_feishu_*`）。
- Agent（MCP/Hermes/HTTP，`agent_provider=none` 默认；`agent_mcp/server.py`、`agent_providers/*`、`agent_tools/*`）。
- LLM/AI 分析（OpenAI 兼容，`ai_service.py`、`api/routes/ai.py`）。
- OpenBB 行情源（`providers/openbb_provider.py`）。
- Go 加速（`bff-gateway`/`market-read-service`/`scan-worker`，URL+token 开关，Python 回退）。
- Rust finance math（11 个 pyfunction，Python 回退）。

### 2.4 历史遗留能力（保留兼容，但应集中标注）
- 研究层策略 7 个、因子层策略 2 个（`strategy_policy.py`，`strong_buy_paused`）。
- 路由重定向：`/emotion→/monitor`、`/low-buy→/playbook`、`/strategy→/backtest`、`/performance→/paper`、`/backtests`、`/research`（[main.py:449](backend/app/main.py:449)、[webRouteDefinitions.tsx:40](frontend/src/app/router/webRouteDefinitions.tsx:40)）。
- `STRATEGY_ALIASES` 旧 key 兼容（[shared.py:307](backend/app/services/low_buy/shared.py:307)）、`v1.py` 版本兼容路由。
- 原生 App（`frontend/android`、`frontend/ios`、`app_mobile`）。
- 6+ 份历史规划/评估文档。

### 2.5 建议隐藏或下沉的能力（详见第 4 节）
- 后台默认开启的研究/模型治理 loop → 默认关、feature flag 显式开启。
- 重复后台 loop 合并。
- one-off 脚本（根 `scripts/` 68 项 + `research/scripts`）归档分类。

---

## 3. 问题总览表

> 严重程度：P0 立即 / P1 高 / P2 中 / P3 低。所有条目均附证据；不确定项标注“需进一步验证”。

| ID | 级别 | 模块 | 问题描述 | 证据 | 影响范围 | 修复建议 | 验收标准 |
|---|---|---|---|---|---|---|---|
| OPS-01 | P1 | 部署/后台 | 默认 compose 下 **Web 容器（4 gunicorn worker）仍运行全部后台调度 loop + 模拟盘自动交易**，与 README“web 只处理 HTTP、由 runtime-worker 单独执行”矛盾；`runtime_worker.py` 实为 RuntimeTask 队列消费者，不跑 task_manager loop/auto-trader | [docker-compose.mysql.yml:106](docker-compose.mysql.yml:106) `WEB_RUNTIME_BACKGROUND_JOBS_ENABLED:-true`；[background_jobs.py:340](backend/app/runtime/background_jobs.py:340) `start_runtime_background_jobs`+`start_auto_trader`；[runtime_worker.py:58](backend/app/workers/runtime_worker.py:58) `while True: claim_next`；[README.md:299](README.md:299) | 低吸全市场扫描/预热/自动交易与 HTTP 请求同容器争用 CPU，影响接口 p95 | 将 Web `WEB_RUNTIME_BACKGROUND_JOBS_ENABLED` 默认 `false`，由独立调度容器跑 task_manager loop + auto-trader；或修正文档并做 CPU 配额隔离 | README/runbook 与 compose 一致；扫描高峰期 Web 容器 CPU 不被后台占满；`/metrics` p95 在扫描窗口无明显抬升 |
| SEC-01 | P1 | 安全/限流 | 限流多 worker 口径不一致：`Dockerfile` 硬编码 `gunicorn -w 4`，但 `app_workers` 默认 1，且 `validate_security_settings` 以 `app_workers` 判定 memory 限流是否放行 | [Dockerfile](Dockerfile) `CMD gunicorn -w 4`；[config.py:88](backend/app/core/config.py:88) `app_workers=1`；[security_config.py:12](backend/app/core/security_config.py:12) | 直接用 Dockerfile（非 compose）跑 + 默认 `memory` 限流后端时，每进程独立计数 → 实际全局限流 ×4 且启动校验放行 | 校验改为基于真实并发推导（gunicorn `--workers`/网关），或 Dockerfile 默认 `-w 1` 并由 `APP_WORKERS` 显式驱动 worker 数 | `-w>1` + `memory` 后端启动即失败；或网关层统一限流并通过校验 |
| STRAT-01 | P1 | 策略/回测 | 8 个生产策略（CORE+AUXILIARY）中较新策略（如 `n_pattern_short_wash`、`mainline_limitup_shrink_retrace_reclaim`）的 24M OOS 样本充分性与分季度稳定性**未逐策略披露**（需进一步验证） | [strategy_policy.py:13](backend/app/services/low_buy/strategy_policy.py:13)；[shared.py:134](backend/app/services/low_buy/shared.py:134) PLAYBOOKS | 样本不足策略若进入优先榜/自动交易，可能过拟合 | 出具“逐策略 buy_now/soft_buy_now 样本量 + 分季度胜率/盈亏比 + 最小样本门槛”报告，未达门槛降为研究层 | 每生产策略有 OOS 样本量与稳定性记录，低于门槛者不进优先榜 |
| DOC-01 | P2 | 文档 | 文档漂移与重叠：`ARCHITECTURE.md` 写 5 个生产策略，代码实为 8；`README.md` 能力清单陈旧；存在 6+ 份重叠规划/评估文档 | [ARCHITECTURE.md:43](ARCHITECTURE.md:43) vs [strategy_policy.py:13](backend/app/services/low_buy/strategy_policy.py:13)；根目录多份 `*_PLAN.md`/评估报告 | 新人/审计认知成本高，易误判能力边界 | 收敛为单一 SSOT（架构+能力矩阵+策略矩阵），策略矩阵从 `strategy_policy.py` 自动生成；历史文档移入 `docs/archive/` | 策略矩阵与代码一致且自动生成；根目录规划文档收敛为 1 份 |
| OPS-02 | P2 | 后台 | **研究/模型治理类 loop 默认开启过多**：主力模型、ML 止盈、月度验证、自进化/漂移/账本、因子挖掘、复盘、通知扫描等在后台默认运行 | [background_jobs.py:340-468](backend/app/runtime/background_jobs.py:340)；[config.py:167-214](backend/app/core/config.py:167) | 生产默认面过大、资源占用与误触发风险 | 研究/模型治理 loop 默认 `false`，经 `feature_flags` 显式开启；区分“生产最小集”与“研究全集”两套默认 | 干净生产模式仅保留行情/低吸物化/watchlist/绩效归档等核心 loop |
| PERF-01 | P2 | 后端复杂度 | `low_buy` ≈130 文件、`services/` ≈100 模块，认知/维护成本高（非缺陷，是结构债） | `backend/app/services/low_buy/`（130 文件）、`backend/app/services/`（100 模块） | 维护成本、回归面、上手门槛 | 本轮不重构：建立模块 owner 表 + 边界说明 + 死代码/未用导出扫描，纳入后续阶段拆分 | 产出模块边界文档与死代码清单；新增/大改文件 <500 行（团队已在执行） |
| FE-01 | P2 | 前端 | 重组件：`MonitorPage.tsx` 997 行；`trading-workspace` 4742 LOC；`backtest` 6418 LOC | [MonitorPage.tsx](frontend/src/features/monitor/MonitorPage.tsx)；`features/trading-workspace/`、`features/backtest/` | 首屏/渲染/可维护性 | 已 lazy + echarts 分包；进一步拆分大页面、研究面板降级为高级态、大表虚拟化 | 关键页面交互拆分为子组件；高级面板按需加载 |
| OPS-03 | P3 | 后台 | **重复后台 loop**：`low_buy_materialization_refresh` 与 `daily_bar_refresh` 均调用同一 `enqueue_latest_data_close_refresh`，同为 300s | [background_jobs.py:303-311](backend/app/runtime/background_jobs.py:303) 与 [:376-386](backend/app/runtime/background_jobs.py:376) | 调度/代码冗余（幂等键已去重执行，无双跑，但易误解、轻微浪费） | 合并为单一 loop，保留一个名称 | 仅一个 loop 负责 close-refresh 入队；行为不变 |
| OPS-04 | P3 | 脚本/仓库 | one-off 脚本累积：根 `scripts/` 68 项 + `research/scripts`，缺少分类 | `ls scripts/`（68）、`research/scripts/` | 仓库噪音、误用风险 | 分类（deploy/qa/backtest/one-off），过期脚本归档，运行入口集中到 `Makefile` | 脚本目录有 README 索引与分类 |
| SEC-02 | P3 | 前端安全 | 内存态 Bearer access token（短期 15min）+ httpOnly refresh cookie 混合 | [base.ts:104](frontend/src/api/base.ts:104)、[base.ts:331](frontend/src/api/base.ts:331) | XSS 下 15 分钟窗口风险（已被强 CSP 缓解） | 维持现状即可，记录为“已缓解”；如需更强可纯 cookie + CSRF token | 记录在安全基线文档；CSP 维持 `script-src 'self'` |
| DATA-01 | P2 | 回测/数据 | 滑点/市场冲击参数与“涨停封板成交概率”等执行假设**缺少来源与敏感性披露**（需进一步验证） | [broker.py:140](backend/app/services/backtest/broker.py:140) `_apply_market_impact`、`_market_impact_rate` 默认 `[0.0008,0.0015,0.003,0.008]`；`_is_limit_up` | 回测净收益对滑点/封板假设敏感 | 在回测报告中加“执行假设来源 + 滑点/冲击敏感性区间”章节 | 报告含敏感性区间；假设有出处或保守上界说明 |

---

## 4. 平台功能瘦身方案

> 原则：**默认关闭、显式开启、文档收敛、边界清晰**；删除从严，先验证。

### 4.1 必须保留（核心生产）
实时监控、低吸生产层（CORE+AUXILIARY）、模拟盘+风控、行情数据管道、24M 回测与报告、策略跟踪、设置/认证/部署。这些是平台价值主线，**不动**。

### 4.2 建议合并
- **后台 loop**：`low_buy_materialization_refresh` + `daily_bar_refresh` → 合一（OPS-03）。
- **规划/评估文档**：`IMPLEMENTATION_PLAN.md`（保留为开发流水账）；`OPTIMIZATION_PLAN.md`/`PROJECT_PLAN.md`/`FINAL_DELIVERY.md`/两份评估报告 → 收敛出 1 份 SSOT（架构+能力矩阵+策略矩阵），其余移入 `docs/archive/`（DOC-01）。
- **策略矩阵**：以 `strategy_policy.py` 为唯一真源，`ARCHITECTURE.md`/前端 `workspaceConstants` 展示从其生成。

### 4.3 建议隐藏到高级/研究模式（默认关）
- 因子挖掘、ML 止盈模型、主力观测模型 ranking、next-day event model、策略自进化/在线学习/漂移/账本、组合优化（BL/Markowitz/启发式）、套利/对冲/仓位研究、另类数据。
- 落地方式：统一走 `feature_flags`（已有 [api/routes/feature_flags.py](backend/app/api/routes/feature_flags.py)），后端对应 loop/端点默认 `false`，前端这些面板归入“研究/高级”入口。

### 4.4 建议插件化（已具雏形，补默认关）
- Go 加速（URL+token 开关，已是）、Agent providers（registry 已是）、飞书通知（webhook 开关）、OpenBB provider、LLM。统一在 `settings` 标注“可选集成，默认关”。

### 4.5 建议删除（从严，均需先验证）
- **当前未发现明显死模块**（IMPLEMENTATION_PLAN 自述近期审查“无未用旧页面/console/TODO/大块重复”）。
- 候选仅为：长期未被 import 的 `research/scripts` 一次性脚本、已被重定向取代的旧文档。

### 4.6 删除风险
- 研究类模块常被回测/报告/优先级链路**间接引用**（如因子参与 scoring、主力模型参与 shadow 与可选加成）。直接删除可能断链。

### 4.7 删除前验证清单（任何删除前逐项执行）
1. `git log -p --follow <file>` 确认最近无活跃引用变更；
2. `rg "import .*<module>|<symbol>"` 全仓搜索（含 `scripts/`、`go-services/` 内部调用、`research/`）；
3. 确认不在 `api/router.py`、`background_jobs.py`、`Makefile`、compose、CI 中被挂载/调度；
4. 跑 `backend pytest` 全量 + `frontend vitest` 全量 + `scripts/qa_smoke.sh` + `scripts/prod_preflight.sh`；
5. 跑一次 24M 报告生成，确认 JSON/MD 字段无缺失；
6. 仅在以上全绿后，先“标记弃用 + 默认关”一个版本，再于下个版本物理删除。

---

## 5. 性能优化方案

> 现状已有不少正确实践，优化以“隔离 + 治理 + 拆分”为主，而非推倒重来。

### 5.1 前端性能
- 现状（良好）：路由 lazy（[webRouteDefinitions.tsx:7](frontend/src/app/router/webRouteDefinitions.tsx:7)）、echarts 细粒度分包（[vite.config.ts](frontend/vite.config.ts) `splitVendorChunks`）、轮询克制（30s `refetchInterval` 仅 3 处 + SSE）。
- 优化动作：
  1. 拆分 `MonitorPage.tsx`(997)/`trading-workspace`/`backtest` 大页面为子组件 + 高级面板按需加载；
  2. 大表格（候选/优先榜/回测明细）引入虚拟滚动；
  3. 顶栏 1s `setInterval`（时钟/pulse）合并为单一计时器，降低重渲染。
- 瓶颈定位：重组件首屏与大表渲染（FE-01）。

### 5.2 后端性能
- 现状（良好）：API 读物化快照、BFF workspace 缓存（[config.py:82](backend/app/core/config.py:82)）、provider 熔断、慢请求日志 `>3000ms`（[main.py:163](backend/app/main.py:163)）、`/metrics` 暴露 p95。
- 优化动作：
  1. **Web 与后台隔离（OPS-01）** —— 最大单点收益；
  2. 打开慢请求日志聚合，定位 `priority_board`/`strategy_tracking` 聚合是否存在 N+1（需进一步验证逐项查询）；
  3. 给热点只读路径补 BFF 缓存 TTL 调优。

### 5.3 数据库性能
- 现状（良好，不是瓶颈）：34 个迁移、337+ 列级 `index=True`、复合索引（如 `ix_user_sessions_user_revoked_expires`）、连接池可配（`db_pool_size=12`/`max_overflow=24`）。
- 优化动作：MySQL 慢查询日志 + 对低吸结果/RuntimeTask/paper trades 等大表跑 EXPLAIN 核验复合索引命中；为日线/成交大表制定归档/分区策略。

### 5.4 回测性能
- 现状（良好）：独立 `backtest-worker` 容器消费回测队列（[docker-compose.mysql.yml:233](docker-compose.mysql.yml:233)），不阻塞 Web。
- 优化动作：24M 全市场扫描的并行度与中间因子缓存复用；产出物（JSON/MD）统一落 `docs/reports/` 并建索引。

### 5.5 部署与运行性能
- 现状（良好）：多阶段镜像（前端/Rust/运行时分层）、非 root 运行、RL/ML 重依赖不入默认镜像（[Dockerfile](Dockerfile)、[requirements.txt](requirements.txt)）。
- 优化动作：Web 去后台任务后下调/匹配 gunicorn worker 与 CPU；冷启动预热加限流，避免启动瞬时打满外部源。

### 5.6 优先级排序
P1：OPS-01（Web/后台隔离）→ P2：5.2.2 慢查询/N+1 治理 → P2：FE-01 重组件拆分 → P3：5.4/5.5 回测并行与镜像启动。

---

## 6. 策略与回测提升方案

### 6.1 策略可信度问题
- 已具备（良好）：四层分层 `CORE/AUXILIARY/RESEARCH/FACTOR`（[strategy_policy.py](backend/app/services/low_buy/strategy_policy.py)）；研究/因子层 `strong_buy_paused`；信号状态机 `buy_now→soft_buy_now→near_entry/observe_confirmed`（[signal_state.py:102](backend/app/services/low_buy/signal_state.py:102)）；`near_entry` 已被排除出生产收益排行（IMPLEMENTATION_PLAN 2026-05-29）。
- 待补：
  1. **逐策略 OOS 样本充分性**（STRAT-01）；
  2. **失效条件集中登记**：每策略的 `notes` 含失效逻辑，但散落在 PLAYBOOKS 文案与 `candidate_rules*`，建议汇总成“策略矩阵：信号/触发/失效/仓位/止盈止损”一页（可从规则常量生成）；
  3. **信号状态验证**：`buy_now/soft_buy_now/confirmed/near_entry` 已在报告分开统计，需补“各状态前瞻收益对照”作为状态阈值的持续校准证据。

### 6.2 回测口径问题（多数已正确）
- 已具备：前视守卫“信号次日入场、区间末日信号不强制同日成交”（[engine_helpers.py:228](backend/app/services/backtest/engine_helpers.py:228)）；T+1 卖出约束（[engine.py:280](backend/app/services/backtest/engine.py:280)）；真实费用模型（佣金/印花/过户，ETF 适配，[fees.py](backend/app/services/paper/fees.py)）；保守滑点 + 参与度市场冲击 + 涨跌停识别 + 停牌拒单（[broker.py](backend/app/services/backtest/broker.py)）；真实组合 max5/max10 资金约束（IMPLEMENTATION_PLAN 2026-05-29）。
- 待补（DATA-01）：执行假设来源与敏感性披露；涨停封板的成交概率假设核验；分钟级 ETF T0 与日线口径一致性核验（`etf/t0_backtest.py` vs `backtest/engine.py`）。

### 6.3 数据质量问题
- 已具备：`data_quality` + `dataset_manifest`（回测内）、免费源降级标记、provider 路由+熔断。
- 待补：交易日历权威源核验；停牌/除权除息对齐核验；缺失值填充策略统一登记为“数据口径文档”。

### 6.4 参数治理问题
- 已具备：参数版本服务（[parameter_version_service.py](backend/app/services/quant/parameter_version_service.py)，495 LOC）、默认参数集、前排过滤 A/B（IMPLEMENTATION_PLAN 2026-05-29）。
- 待补：把 A/B 从“前排过滤”推广为**全策略标准 A/B 流程**；参数**回滚 runbook** + 回滚演练；参数变更落审计（`operation_audit`）。

### 6.5 模拟盘验证问题
- 已具备：自动交易 + 风控熔断（[risk_circuit.py](backend/app/services/paper/risk_circuit.py)）+ 绩效归档 + shadow→生产 promotion 数值门槛（`main_force_model_*_min`，[config.py:188](backend/app/core/config.py:188)）。
- 待补：promotion gate 的**统计显著性判定文档化**（样本量/置信区间），并在 UI 暴露“为何未晋级”原因（已有 `no_sample_reason` 雏形）。

### 6.6 24 个月回测补强方案
- 已具备（良好）：可复现脚本（`scripts/strategy_24m_*`、`scripts/low_buy_market_backtest_*`）、JSON+Markdown、未成交/退出原因/市场分段/季度表现、报告口径收口。
- 补强：
  1. 形成《回测可信度验收清单》（费用/滑点/封板/T+1/前视/样本/分段，逐项打勾）；
  2. 建立 `docs/reports/` 产物索引（命令 → JSON → MD 一一对应）；
  3. 每个生产策略输出“样本量 + 分季度稳定性 + 失效样本占比”，作为进/退优先榜的硬门槛。

---

## 7. 安全与权限提升方案

> 现状安全基线相当完整，本节以“收口默认面 + 角色细分 + 轮换流程”为主。

### 7.1 用户权限
- 现状：cookie httpOnly + secure + samesite（[auth.py:194](backend/app/api/routes/auth.py:194)）、登录锁定、MFA/TOTP、`auth_allowed_usernames` 白名单。
- 待补：角色细分（当前主要是 `admin` vs 普通用户，[admin_auth.py:73](backend/app/core/admin_auth.py:73)）。建议引入“研究用户”角色，把研究/高级面板与生产用户隔离。

### 7.2 管理员权限
- 现状（良好）：`X-Admin-Token` 或 bearer-admin 角色；未配置 token 直接 503；`compare_digest`；`/metrics` 受 `require_admin_auth` 保护（[main.py:327](backend/app/main.py:327)）。
- 待补：管理操作全量入审计（已有 `OperationAuditMiddleware`，核验覆盖面）。

### 7.3 Agent/AI 权限
- 现状（良好）：工具 registry 分 `read/write/notify`（[registry.py:122](backend/app/agent_tools/registry.py:122)），`agent_enable_write_tools/notify_tools` 默认 `false`，`agent_provider=none` 默认，审计 + 速率限制（`agent_tools/audit.py`、`rate_limit.py`、`policy.py`）。
- 待补：生产环境显式声明“Agent 默认只读、不下单、不改参数、不执行 shell”（与 ARCHITECTURE 安全边界一致），并加冒烟用例固化。

### 7.4 敏感配置
- 现状（良好）：`secret_field_crypto`、`totp_secret_crypto`、启动校验 `TQUANT_SETTINGS_ENCRYPTION_KEY` 与 `AUTH_SECRET_KEY` 必须解耦且 ≥64 位（[security_config.py:26](backend/app/core/security_config.py:26)）、双持久化（DB + `runtime.env`）。
- 待补：`runtime.env` 文件权限与落盘加密核验；密钥**轮换流程**文档化；确认敏感 key 不进日志（结构化日志脱敏核验）。

### 7.5 审计
- 现状：`operation_audit_middleware` + `operation_audit_entities`；Agent 审计 + `/metrics` 暴露调用计数。
- 待补：审计覆盖矩阵（哪些写操作必审）+ 留存周期策略。

### 7.6 限流
- 现状：全局限流（[main.py:125](backend/app/main.py:125)）+ 请求体上限 + Agent 速率限制；生产强制非 memory 后端（校验）。
- 待补：**SEC-01 多 worker 口径修复**；限流后端 Redis 化在 compose 已默认，补单测覆盖多 worker 行为。

### 7.7 生产默认关闭项（清单）
建议在“干净生产模式”下默认 `false`：`main_force_model_*`（保留只读展示可选）、`paper_exit_model_enabled`、`strategy_validation_monthly_enabled`、`evolution_*`、`factor_mining`、`market_review_enabled`、`notification_signal_scan_enabled`（无 webhook 时自然空跑，但建议显式关）、`platform_autopilot_enabled`。`enable_deep_rl*` 已默认关、依赖不入镜像（保持）。

---

## 8. 测试补强方案

### 8.1 当前已有测试（基础扎实）
- 后端 151 个 pytest：paper(15)/low_buy(15)/market(14)/strategy(13)/main_force(9)/etf(7)/agent(7)/backtest(4)/security(3)/auth(3)/ml(3)/totp/sse/bff/go/rust/provider…
- 前端 24 个 vitest（含 `BacktestDashboard.test.tsx`、`StrategyTrackingPage.test.tsx`、`PaperTradingPage.test.tsx`）。
- 脚本：`qa_smoke.sh`、`ui_smoke.sh`、`prod_preflight.sh`、`app_api_smoke.sh`、`verify_go_rust_performance_acceptance.py`、`verify_backend_refactor_foundation.sh`。

### 8.2 缺失的关键测试（建议补）
1. **部署拓扑集成测试**：验证后台 loop 与 auto-trader 只在指定容器运行（OPS-01 回归）。
2. **限流多 worker 行为测试**：`-w>1` + memory 后端应失败/被网关接管（SEC-01 回归）。
3. **后台 loop 幂等/去重测试**：`enqueue_latest_data_close_refresh` 重复入队不双跑（OPS-03 回归）。
4. **回测口径回归**：前视守卫、T+1、费用、滑点、涨跌停拒单、`near_entry` 不进生产排行的断言（部分已在 `test_low_buy_backtest_isolation.py`/`test_strategy_24m_report_sections.py`，建议补全口径矩阵）。
5. **模拟盘自动交易约束 E2E**：max_orders/min_score/T+1/熔断触发链路。
6. **provider 降级契约测试**：主源失败 → 熔断 → 回退 → 数据质量标记。
7. **Agent 只读边界测试**：write/notify 默认关时端点拒绝。

### 8.3 最小冒烟测试集（每次提交/PR）
```bash
scripts/qa_smoke.sh                       # 构建+接口+分析+设置+回测冒烟
backend/.venv/bin/python -m pytest backend/tests \
  -k "security or auth or paper or low_buy or backtest_isolation or strategy_24m_report" -q
cd frontend && npm run test -- --run        # 关键 vitest
curl -fsS localhost:8000/readyz             # 就绪检查
```

### 8.4 发布前完整测试集（每次发布）
```bash
scripts/prod_preflight.sh                   # 版本一致性+前端构建+后端编译+单端口验活
backend/.venv/bin/python -m pytest backend/tests -q   # 全量后端
cd frontend && npm run test -- --run                  # 全量前端
python scripts/verify_go_rust_performance_acceptance.py   # Go/Rust 验收
scripts/ui_smoke.sh http://127.0.0.1:18080            # 浏览器级四页截图
# + 8.2 中限流/鉴权/拓扑/回测口径用例
# + 24M 报告 schema 回归（JSON/MD 字段齐全）
```

### 8.5 回归测试优先级
鉴权/限流/费用口径/前视守卫/T+1（最高）→ 策略分层/信号状态/promotion gate → 报告字段 schema → 前端关键页面渲染。

---

## 9. 分阶段实施路线图

### 第 1 阶段：止血与瘦身（1–2 周）
- OPS-01 Web/后台隔离（compose Web `WEB_RUNTIME_BACKGROUND_JOBS_ENABLED=false` + 独立调度容器或文档校正）。
- SEC-01 限流多 worker 口径修复。
- OPS-02 研究/模型治理 loop 默认关，区分“生产最小集 / 研究全集”。
- OPS-03 合并重复 loop。
- DOC-01 文档 SSOT 收敛 + 策略矩阵自动生成 + 历史文档归档。

### 第 2 阶段：性能与稳定性（2–4 周）
- 慢查询/N+1 治理（priority_board/strategy_tracking）。
- FE-01 重组件拆分（Monitor/Workspace/Backtest）+ 大表虚拟化。
- 回测扫描并行/因子缓存；产物索引。
- 镜像/启动优化（worker 数匹配、预热限流）。

### 第 3 阶段：策略与回测可信度（3–6 周）
- STRAT-01 逐策略 24M OOS 样本与稳定性门槛。
- DATA-01 执行假设来源 + 滑点/封板敏感性披露。
- 《回测可信度验收清单》落地。
- 参数 A/B 标准化 + 回滚 runbook + 审计落库。

### 第 4 阶段：产品体验与高级能力（4–8 周）
- 用户角色细分（普通/研究/管理）+ 研究/高级态前端入口。
- 高级能力插件化收口（因子/ML/优化/套利/RL）。
- Agent 落地范围与只读边界固化 + 冒烟用例。

### 第 5 阶段：部署、监控与长期治理（持续）
- Prometheus/Grafana 告警规则核验（`docker-compose.monitoring.yml`）。
- 回滚手册 + 密钥轮换流程 + 大表归档策略。
- 死代码扫描常态化 + 模块 owner 制。

---

## 10. 可执行任务清单

> 每个任务：标题 / 优先级 / 涉及文件 / 步骤 / 验收 / 风险。**均不在本轮执行，仅为实施蓝图。**

### T1 Web 与后台任务隔离
- 优先级：P1
- 涉及：`docker-compose.mysql.yml`、`backend/app/runtime/background_jobs.py`、`README.md`、`PRODUCTION_RUNBOOK.md`
- 步骤：①Web 服务 `WEB_RUNTIME_BACKGROUND_JOBS_ENABLED` 默认 `false`；②新增/复用独立“scheduler”容器跑 task_manager loop + auto-trader（与 runtime-worker 队列消费分离）；③同步 README/runbook。
- 验收：扫描高峰 Web 容器 CPU 不被后台占满；`/metrics` p95 平稳；文档与 compose 一致。
- 风险：调度容器需保证全局唯一（沿用 `fcntl` 选主或单实例部署）。

### T2 限流多 worker 口径修复
- 优先级：P1
- 涉及：`backend/app/core/security_config.py`、`Dockerfile`、`backend/app/core/rate_limit.py`
- 步骤：校验改为基于真实并发（读取 gunicorn worker 数/网关），或 Dockerfile 以 `APP_WORKERS` 驱动 `-w`，并令二者一致。
- 验收：`-w>1` + memory 后端启动即失败；compose（Redis 后端）通过。
- 风险：需确保不误伤单 worker 本地开发。

### T3 生产默认最小化（研究 loop 默认关）
- 优先级：P1
- 涉及：`backend/app/core/config.py`、`backend/app/runtime/background_jobs.py`、`api/routes/feature_flags.py`
- 步骤：研究/模型治理 loop 默认 `false`；新增“生产最小集 vs 研究全集”profile；前端高级面板读取 feature flag。
- 验收：干净生产模式仅核心 loop 运行；研究功能显式开启可用。
- 风险：误关核心 loop；需用 T1 拓扑测试兜底。

### T4 逐策略 24M OOS 样本与稳定性门槛
- 优先级：P1
- 涉及：`scripts/strategy_24m_*`、`backend/app/services/low_buy/strategy_policy.py`、`docs/reports/`
- 步骤：为 8 个生产策略产出样本量/分季度胜率盈亏比/失效占比；设最小门槛；未达标降研究层。
- 验收：每生产策略有 OOS 记录；优先榜仅含达标策略。
- 风险：样本不足策略需降级，可能影响现有展示，先 shadow 再切换。

### T5 文档 SSOT 收敛 + 策略矩阵自动生成
- 优先级：P2
- 涉及：`ARCHITECTURE.md`、`README.md`、根目录 `*_PLAN.md`/评估报告、`strategy_policy.py`
- 步骤：建 1 份 SSOT；策略矩阵脚本从 `strategy_policy.py`+PLAYBOOKS 生成；历史文档移 `docs/archive/`。
- 验收：策略矩阵与代码一致且可重生成；根目录文档收敛。
- 风险：低（纯文档），注意保留历史可追溯。

### T6 合并重复后台 loop
- 优先级：P3
- 涉及：`backend/app/runtime/background_jobs.py`
- 步骤：合并 `low_buy_materialization_refresh` 与 `daily_bar_refresh`。
- 验收：仅一个 loop 入队 close-refresh，行为不变（含幂等去重单测）。
- 风险：极低。

### T7 慢查询 / N+1 治理
- 优先级：P2
- 涉及：`services/low_buy/priority_board.py`、`services/strategy_tracking*`、MySQL 慢日志
- 步骤：开慢日志；对热点聚合 EXPLAIN；消除 N+1（批量查询/预加载）。
- 验收：热点接口查询次数与耗时下降；p95 改善。
- 风险：批量化改写需回归正确性。

### T8 前端重组件拆分 + 大表虚拟化
- 优先级：P2
- 涉及：`features/monitor/MonitorPage.tsx`、`features/trading-workspace/*`、`features/backtest/*`
- 步骤：拆子组件、研究面板按需加载、候选/榜单/回测明细虚拟滚动。
- 验收：关键页面交互流畅；首屏 chunk 下降。
- 风险：交互回归，需补 vitest。

### T9 回测可信度验收清单 + 敏感性披露
- 优先级：P2
- 涉及：`services/backtest/broker.py`、`scripts/low_buy_market_backtest_*`、`docs/reports/`
- 步骤：报告新增执行假设来源 + 滑点/封板敏感性区间；产出验收清单。
- 验收：报告含敏感性区间与假设出处；清单逐项可勾。
- 风险：低（增量披露）。

### T10 参数 A/B 标准化 + 回滚 runbook
- 优先级：P2
- 涉及：`services/quant/parameter_version_service.py`、`operation_audit`、`docs/`
- 步骤：A/B 从前排过滤推广为全策略流程；参数变更落审计；写回滚手册并演练。
- 验收：参数可一键回滚并留审计；A/B 报告标准化。
- 风险：版本迁移需兼容历史参数。

### T11 用户角色细分（普通/研究/管理）
- 优先级：P2
- 涉及：`core/admin_auth.py`、`services/auth_service.py`、前端路由守卫
- 步骤：引入“研究”角色；研究/高级面板按角色可见。
- 验收：普通用户看不到研究面板；研究用户无管理权限。
- 风险：权限回归，需鉴权用例覆盖。

### T12 测试补强（拓扑/限流/口径/E2E）
- 优先级：P1（与 T1/T2 并行）
- 涉及：`backend/tests/`、`frontend` vitest、`scripts/`
- 步骤：补 8.2 列出的 7 类测试；接入最小冒烟集与发布前完整集到 `Makefile`/CI。
- 验收：最小冒烟集与发布集可一键运行并纳入 CI。
- 风险：低，纯增测。

### T13 one-off 脚本归档分类
- 优先级：P3
- 涉及：`scripts/`、`research/scripts/`、`Makefile`
- 步骤：按 deploy/qa/backtest/one-off 分类，加 README 索引，过期脚本归档。
- 验收：脚本目录有索引；常用入口收敛到 `Makefile`。
- 风险：低，注意保留被引用脚本。

---

## 11. 最建议优先做的 10 个任务

> 排序综合“风险收益比 + 解锁后续 + 与当前阶段（phase4/5）契合度”。

1. **T1 Web 与后台任务隔离（P1）** —— 单点性能/稳定性收益最大，直接消除 HTTP 与扫描/自动交易争用。
2. **T2 限流多 worker 口径修复（P1）** —— 安全口径硬伤，修复成本低、影响面明确。
3. **T3 生产默认最小化（P1）** —— 一步把“干净生产模式”落地，缩小默认攻击面与资源占用。
4. **T12 测试补强：拓扑/限流/回测口径（P1）** —— 给 T1/T2/T3 提供回归护栏，必须并行。
5. **T4 逐策略 24M OOS 样本门槛（P1）** —— 策略可信度的根本，决定哪些策略可进优先榜/自动交易。
6. **T5 文档 SSOT + 策略矩阵自动生成（P2）** —— 消除认知/审计成本，止住文档漂移。
7. **T6 合并重复后台 loop（P3，顺手做）** —— 极低成本，随 T1 一并清理。
8. **T9 回测可信度验收清单 + 敏感性披露（P2）** —— 把已具备的扎实回测能力“证据化”，对外可解释。
9. **T7 慢查询 / N+1 治理（P2）** —— 在 T1 隔离后进一步压低后端尾延迟。
10. **T8 前端重组件拆分（P2）** —— 改善体验与可维护性，为研究/高级态降级铺路。

---

### 附：本轮核验要点（用于复核结论可信度）
- 上一版 4 个 P0 全部闭环（agent 鉴权 / net_win_rate / cookie / admin 503）—— 见第 1 节表。
- 回测前视守卫、T+1、费用、滑点+冲击、涨跌停、停牌拒单、真实组合约束 —— 均有代码证据。
- 安全：CSP/HSTS/内部服务 token/限流/请求体上限/审计/启动校验/密钥解耦 —— 均有代码证据。
- 依赖与镜像：默认精简、ML/RL 为可选 extras、不入默认镜像、非 root —— 有证据。
- 索引/迁移：337+ 列索引 + 复合索引 + 34 迁移 —— 数据库非瓶颈。
- 不确定项（已在表中标注“需进一步验证”）：OPS-01 设计意图、STRAT-01 逐策略样本、DATA-01 执行假设来源、5.2 N+1 具体位置。
