{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for local status inspection.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
